/// Paths for Welcome screen card illustrations.
///
/// Replace the PNG files under `assets/images/welcome/` with your own art.
/// Keep the same filenames, or update these constants + `pubspec.yaml`.
abstract final class WelcomeAssets {
  static const tapMemberCard = 'assets/images/welcome/rfid.png';
  static const register = 'assets/images/welcome/register.png';

  /// Set to `false` after you replace the placeholder PNGs with real art
  /// so the "IMAGE / SVG" label is hidden.
  static const showPlaceholderLabels = false;
}
